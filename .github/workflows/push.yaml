name: push-workflow
on: push

jobs:
  push-job:
    runs-on: ubuntu-latest
    steps:
      - name: Step1
        run: echo hello world
      - name: step2
        run: |
          echo hello world
          echo github action